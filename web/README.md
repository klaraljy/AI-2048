# 前端（桌面浏览器）

> **状态：空。** 前端尚未开始。

## 计划

无框架、无构建：原生 HTML / CSS / JS，ES module。

## 与参考原型的关系

参考实现：`F:\AI编程\2048-ai2\2048-ai`。它**不是**本项目的一部分，只作参考与对照基线。

| 原型文件 | 处置 |
|---|---|
| `index.html` + `css/style.css` | 可作视觉基线。2…2048 是经典 Gabriele Cirulli 调色板，配色可留 |
| `renderer.js` | **必须重写**。它每步 `innerHTML = ""` 重建 16 个节点，方块没有身份 → 滑动动画在这一层不可能实现 |
| `input.js` | 重写。原型只有键盘，触屏设备上完全无法操作 |

## 硬要求

- **不得内联 AI 逻辑**：AI 决策一律来自引擎。
- **传输层必须可换**：`js/ai-transport.js` 是唯一允许知道"怎么连引擎"的文件。
  引擎连不上时前端要降级到 JS 基线运行并给出可见提示，**不能白屏**。
- 前端没有自动化测试，验收靠 `../AGENTS.md` 里那份手工清单，**每次改动都要实际走一遍**。

## 已知缺口（来自原型审查，全部要补）

方块滑动 / 合并弹跳 / 新块出现 / 得分浮字 / 音效（原型为零）/ 触屏滑动 /
撤销（原型按钮是假的）/ 最高分持久化 / 游戏结束遮罩 / 输入锁 / `prefers-reduced-motion`。

完整对照表见 `../docs/brief.md` 的「前端缺口清单」。
