# ---- 构建产物 ----
# build*/ 而不是 build/ ：本项目用 engine\build（普通）与 engine\build-tests（含 GoogleTest）
# 两个构建目录，只写 build/ 会漏掉后者，GoogleTest 的整个检出会被提交进去。
build*/
out/
cmake-build-*/
CMakeCache.txt
CMakeFiles/
CMakeUserPresets.json
_deps/
*.ninja
.ninja_deps
.ninja_log

# ---- 编译产物 ----
*.o
*.obj
*.a
*.lib
*.so
*.dll
*.dylib
*.exe
*.pdb
*.ilk
*.exp

# ---- C++ / CMake 工具 ----
compile_commands.json
.cache/
ccache/

# ---- 测试与覆盖率 ----
Testing/
*.gcda
*.gcno
*.gcov
coverage/

# ---- 跑批与调参的可再生成产物 ----
# 注意：benchmarks/ 是"基准种子集"，是可复现性资产，**必须提交**。
# 只忽略跑批产生的大体量中间数据与训练权重。
benchmarks/raw/
*.log
*.csv
*.jsonl
*.profraw
*.profdata

# 训练权重：体积可达数百 MB，不进版本管理。
# 获取/再生成方式写在 engine/models/README.md 里，不得静默提交大文件。
engine/models/
# 但那个说明本身要进版本管理 —— 父目录被忽略时子文件默认也进不去，
# 所以要先取消忽略目录（用 /* 只放行一层，不递归）再放行文件。
!engine/models/
engine/models/*
!engine/models/README.md
*.weights
*.w
*.bin

# ---- Android ----
android/.gradle/
android/local.properties
android/build/
android/app/build/
android/.idea/
android/captures/
*.apk
*.aab
*.keystore
*.jks
gradle-home/

# 前端资源在 Android 侧的副本：与 web/ 是同一份内容，但**必须提交**。
# 理由：APK 里要的是 assets，如果它在 .gitignore 里，clone 下来直接构建
# 会得到一个没有前端的空壳 APK —— 那种失败很难一眼看出原因。
# 代价是这份副本可能落后于 web/，所以：
#   ⚠️ 改动 web/ 之后必须重新同步到 android/app/src/main/assets/web/
# 同步命令记在 android/README.md 里。
# （注意：不要在这里 ignore 它。曾想用 assets/web/ 忽略，那会直接做出空壳包。）

# ---- Node（前端无构建，仅防意外） ----
node_modules/
package-lock.json

# ---- 编辑器与系统 ----
.vscode/
.idea/
*.swp
*~
Thumbs.db
Desktop.ini
desktop.ini

# ---- 临时文件 ----
# 项目临时文件统一放 E:\DeepSeekProjects\_tmp\AI-2048\，不应出现在这里。
# 这条是兜底：万一有残留，不要提交。
tmp/
temp/
*.tmp
*.bak
