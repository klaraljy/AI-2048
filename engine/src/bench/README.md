// 跑批：随机种子、统计、报表。
//
// 待实现（里程碑 5）：
//   - 读取 benchmarks/ 下的种子集，多线程跑完并汇总
//   - 输出：平均分、最大块分布、到达 2048/4096/8192 的比例、per-move 耗时
//   - 结果写入 docs/results/，文件名带种子集版本号与本次改动的 tag
//
// 硬约束：并行不得改变结果 —— 第 N 局的种子由种子集决定，与它跑在哪个线程无关。
// 详见 AGENTS.md 的「跑批规模」与「完整验证」。
