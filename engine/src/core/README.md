// 游戏规则：棋盘、移动、合并、生成、计分。
//
// 这是**规则引擎**，改动它等于换了一个游戏：必须同步提升 RulesetVersion()
// 并重建 benchmarks/ 下的基准种子集。
//
// 待实现（里程碑 1）：
//   - 64-bit 位棋盘（4 bit/格 存 exponent）+ 行查找表走子
//   - 引擎自己的种子化 PRNG（确定性；不得用 std::random_device）
//   - 每步返回方块轨迹（from/to/是否合并），供前端做滑动动画
//   - 并行跑批时"第 N 局的种子由种子集决定"，与线程无关
//
// 详见 docs/brief.md 的里程碑 1 与 AGENTS.md 的「规则即契约」。
